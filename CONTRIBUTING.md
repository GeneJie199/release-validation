# Contributing

Run `gofmt -w .`, `go test ./...`, and `go vet ./...`. New check types need timeout handling, bounded evidence, and success and failure tests. Preserve deterministic decisions and backward compatibility of `releaseguard.report/v1`.
