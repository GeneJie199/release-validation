# Security

Release plans may execute commands. Review them as executable code, store them in version control, and run ReleaseGuard with the least privilege needed. The report UI binds to loopback by default. Report vulnerabilities through GitHub Security Advisories.
